sudo: required
services:
  - docker

before_install:
  - docker build -t nikhil609651r/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run nikhil609651r/react-test npm test -- --coverage

after_success:
  - docker build -t nikhil609651r/multi-client ./client
  - docker build -t nikhil609651r/multi-nginx ./nginx
  - docker build -t nikhil609651r/multi-server ./server
  - docker build -t nikhil609651r/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push nikhil609651r/multi-client
  - docker push nikhil609651r/multi-nginx
  - docker push nikhil609651r/multi-server
  - docker push nikhil609651r/multi-worker

